<?
# you might need to change this for mirrors not on their own virtual host
$root_url = "/";
$base_url = ereg_replace("[^/]*\$","",$PHP_SELF);

# see Mirrors-htdig.tgz
$htsearch_prog="/local/htdig/bin/htphp.sh";
?>
